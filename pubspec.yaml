name: flutter_ai_chart_water_consumption_prediction
description: "Syncfusion Flutter AI Chart Water Consumption Prediction"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  syncfusion_flutter_charts: ^28.1.36
  google_generative_ai: ^0.4.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/ai_assist_view.png
